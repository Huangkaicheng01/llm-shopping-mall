# 使用说明：复制本文件为同目录下的 .env，再填写下方变量。
#
# ---------- 会话与安全 ----------
# Flask 登录会话签名密钥（生产环境务必设为长随机串）
# SECRET_KEY=
# 应用启动时会自动读取 .env（.env 已加入 .gitignore，请勿把密钥提交到 Git）。
#
# ---------- 商品数据库（SQLite）----------
# 库文件：instance/catalog.db
# 初始/重灌数据：data/catalog_seed.sql（不要从运行时读 products.csv）
# 需要按种子文件清空并重写库时：CATALOG_RESEED=1 启动一次后去掉
#
# 如何查看与管理数据库：
#   · 图形：安装 DB Browser for SQLite（https://sqlitebrowser.org/）→ 打开 instance/catalog.db
#   · VS Code：安装 SQLite / SQLite Viewer 等扩展 → 打开同一 db 文件
#   · 命令行：sqlite3 instance/catalog.db   然后  .tables   SELECT * FROM products LIMIT 5;
#
# 若你改了 data/products.csv 想重新生成种子 SQL：python tools/gen_catalog_seed.py
#
# ---------- 大模型（任选一种配置方式）----------
# 通义：LLM_API_KEY + LLM_API_BASE + LLM_MODEL；不要与无效 OPENAI_API_KEY 并存
# OpenAI 官方：OPENAI_API_KEY + OPENAI_MODEL

# LLM_API_KEY=
# LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MODEL=qwen-turbo

# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
